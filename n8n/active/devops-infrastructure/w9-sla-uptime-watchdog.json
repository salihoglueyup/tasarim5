{
  "name": "W9 — Master SLA & Uptime Watchdog",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 2
            }
          ]
        }
      },
      "id": "schedule-trigger-2m",
      "name": "Her 2 Dakikada Bir Tetikle",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.1,
      "position": [
        220,
        300
      ]
    },
    {
      "parameters": {
        "url": "http://web:3001/api/health",
        "options": {
          "timeout": 5000
        }
      },
      "id": "check-health-api",
      "name": "GET /api/health Kontrolü",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        440,
        300
      ],
      "onError": "continueRegularOutput"
    },
    {
      "parameters": {
        "jsCode": "const response = $json;\nconst isHttpOk = !response.error && (response.status === 'pass' || response.status === 'warn');\nconst isHealthy = Boolean(response.healthy) && isHttpOk;\n\nconst dbStatus = response.services?.database?.status || 'fail';\nconst cacheStatus = response.services?.cache?.status || 'fail';\nconst heapUsedMb = response.services?.system?.heapUsedMb || 0;\nconst latencyMs = response.latencyMs || 0;\nconst uptimeSeconds = response.uptimeSeconds || 0;\n\n// Alarm eşik kontrolleri (SLA %99.9 standardı)\nconst isDbFailing = dbStatus !== 'pass';\nconst isCacheFailing = cacheStatus === 'fail';\nconst isRamCritical = heapUsedMb > 950;\nconst isLatencyCritical = latencyMs > 3500;\n\nconst alertRequired = !isHealthy || isDbFailing || isCacheFailing || isRamCritical || isLatencyCritical;\n\nlet severity = 'OK';\nlet reason = 'Tüm servisler operasyonel.';\n\nif (!isHealthy) {\n  severity = 'CRITICAL';\n  reason = 'Sistem sağlığı FAIL durumunda veya /api/health yanıt vermiyor!';\n} else if (isDbFailing) {\n  severity = 'CRITICAL';\n  reason = 'PostgreSQL veritabanı bağlantısı koptu!';\n} else if (isRamCritical) {\n  severity = 'WARNING';\n  reason = `Node.js RAM tüketimi kritik eşiği aştı: ${heapUsedMb} MB`;\n} else if (isLatencyCritical) {\n  severity = 'WARNING';\n  reason = `Sistem gecikmesi çok yüksek: ${latencyMs} ms`;\n}\n\nreturn [{\n  json: {\n    timestamp: new Date().toISOString(),\n    isHealthy,\n    alertRequired,\n    severity,\n    reason,\n    dbStatus,\n    cacheStatus,\n    heapUsedMb,\n    latencyMs,\n    uptimeHours: (uptimeSeconds / 3600).toFixed(2),\n    rawResponse: response\n  }\n}];"
      },
      "id": "evaluate-health",
      "name": "RFC 8485 Sağlık Analizi",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        660,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "boolean": [
            {
              "value1": "={{ $json.alertRequired }}",
              "value2": true
            }
          ]
        }
      },
      "id": "filter-alert",
      "name": "Acil Durum Var mı?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        880,
        300
      ]
    },
    {
      "parameters": {
        "fromEmail": "bildirim@aloyonetim.com.tr",
        "toEmail": "istanbul@aloyonetim.com.tr",
        "subject": "=🚨 [ALARM: {{ $json.severity }}] Alo Yönetim Sistem Uyarısı",
        "emailType": "text",
        "text": "=Alo Yönetim DevOps Watchdog Acil Durum Uyarısı\n\nZaman: {{ $json.timestamp }}\nDurum: {{ $json.severity }}\nNeden: {{ $json.reason }}\n\nMetrikler:\n- PostgreSQL Durumu: {{ $json.dbStatus }}\n- Redis Durumu: {{ $json.cacheStatus }}\n- Bellek Tüketimi: {{ $json.heapUsedMb }} MB\n- Yanıt Gecikmesi: {{ $json.latencyMs }} ms\n- Çalışma Süresi: {{ $json.uptimeHours }} saat\n\nLütfen sunucu durumunu (docker compose ps) ve logları kontrol ediniz."
      },
      "id": "send-alert-email",
      "name": "Acil E-Posta Gönder",
      "type": "n8n-nodes-base.emailSend",
      "typeVersion": 2.1,
      "position": [
        1120,
        200
      ]
    },
    {
      "parameters": {
        "jsCode": "console.log(`[Watchdog OK] ${$json.timestamp} - Sistem sorunsuz. Uptime: ${$json.uptimeHours}h, RAM: ${$json.heapUsedMb}MB`);\nreturn [{ json: { status: 'OK', loggedAt: $json.timestamp } }];"
      },
      "id": "log-heartbeat",
      "name": "Sessizce Logla",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1120,
        400
      ]
    }
  ],
  "connections": {
    "Her 2 Dakikada Bir Tetikle": {
      "main": [
        [
          {
            "node": "GET /api/health Kontrolü",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "GET /api/health Kontrolü": {
      "main": [
        [
          {
            "node": "RFC 8485 Sağlık Analizi",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "RFC 8485 Sağlık Analizi": {
      "main": [
        [
          {
            "node": "Acil Durum Var mı?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Acil Durum Var mı?": {
      "main": [
        [
          {
            "node": "Acil E-Posta Gönder",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Sessizce Logla",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveExecutionProgress": false,
    "saveFailedExecutions": true
  },
  "id": "w09SlaWatchdog01"
}
